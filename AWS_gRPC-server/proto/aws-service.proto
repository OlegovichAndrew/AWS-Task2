syntax = "proto3";
package proto;
option go_package = "./;proto";


message Response {
  bytes fileChunk = 1;
}

message Request {
  string fileName = 1;
  string fileBucket = 2;
}

service AWSService {
  rpc Download(Request) returns (stream Response);
}
